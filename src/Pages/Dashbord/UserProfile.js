import React from 'react';

const UserProfile = () => {
    return (
        <div>
            <h1 className='text-center text-2xl'>User Dashboard</h1>
        </div>
    );
};

export default UserProfile;